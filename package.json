{
  "name": "yt-dlp-app",
  "private": true,
  "packageManager": "pnpm@10.16.0",
  "scripts": {
    "dev": "pnpm --filter app dev",
    "dev:renderer": "pnpm --filter ./app/renderer dev",
    "dev:electron": "pnpm --filter ./app/electron dev",
    "build": "pnpm --filter app build",
    "lint": "pnpm lint:js && pnpm lint:py",
    "lint:js": "biome check .",
    "lint:py": "app/downloader/.venv/bin/ruff check app/downloader",
    "prepare": "husky"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.7",
    "husky": "^9.1.7"
  }
}
