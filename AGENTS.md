# Codexへのルール

1. git操作のコマンドは毎回実行する前に私に許可をとってください。


## 次の計画

1. 保存基盤を作る
     SQLite を用意して、動画ファイル保存先との対応を持たせる。
     この段階では url, title, status, file_path, created_at くらいで十分です。
2. ダウンロード処理を最小で通す
     まずは 1 本の URL を受けて、実際に保存して、DB に記録できるところまで作る。
     UI はまだ簡素でいいです。
3. UI に一覧表示を出す
     保存済みや進行中のジョブを一覧表示する。
     ここで SQLite と UI がつながります。
4. 進捗表示を入れる
     yt-dlp の標準出力を parse して %, 速度, 残り時間 を UI に出す。
     この段階で IPC 設計が重要になります。
5. フォーマット・画質指定を入れる
     mp4/mp3, 解像度, 音声のみ などのオプションを UI から渡せるようにする。
     これはダウンロード基盤が固まってからの方が安全です。
6. 複数並列ダウンロードに広げる
     ジョブキュー管理を入れて、同時実行数を制御する。
     最初から無制限並列にすると壊れやすいので、同時2本 などから始めるのがいいです。